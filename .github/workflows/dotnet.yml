name: Build and Publish

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 7.0.x

    - name: Restore dependencies
      run: dotnet restore --runtime win-x64 /p:BuildNumber=${{ github.run_number }}
    
    - name: Build
      run: dotnet build --configuration Release --runtime win-x64 --no-restore /p:BuildNumber=${{ github.run_number }}
    
    - name: Publish
      run: dotnet publish --configuration Release --no-build --self-contained -r win-x64 -p:PublishSingleFile=true -p:BuildNumber=${{ github.run_number }} -o ./publish

    - name: Upload Artifact
      uses: actions/upload-artifact@v2
      with:
        name: ArbitraryImageResizer
        path: ./publish/ArbitraryImageResizer.exe

    - name: Get Version
      id: getversion
      run: |
        $version = [xml](Get-Content ./arbitrary-image-resizer.csproj)
        echo "RELEASE_VERSION=v$($version.Project.PropertyGroup.Version)" >> $GITHUB_OUTPUT
      shell: pwsh

    - name: Debug Version
      run: echo "Release Version is ${{ steps.getversion.outputs.RELEASE_VERSION }}"

    - name: Create Release
      run: |
        $releaseResponse = Invoke-RestMethod `
        -Headers @{Authorization="token ${{ secrets.RELEASE_TOKEN }}"} `
        -Uri https://api.github.com/repos/${{ github.repository }}/releases `
        -Method POST `
        -Body (ConvertTo-Json @{
            tag_name = "${{ steps.getversion.outputs.RELEASE_VERSION }}"
            name = "Release ${{ steps.getversion.outputs.RELEASE_VERSION }}"
            draft = $false
            prerelease = $false
        })
        $uploadUrl = $releaseResponse.upload_url -replace "{?name,label}", "?name=ArbitraryImageResizer.exe"
        Invoke-RestMethod `
        -Headers @{Authorization="token ${{ secrets.GITHUB_TOKEN }}"; "Content-Type"="application/octet-stream"} `
        -Uri $uploadUrl `
        -Method POST `
        -InFile ./publish/ArbitraryImageResizer.exe
      shell: pwsh
